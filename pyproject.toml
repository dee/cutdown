[project]
name = "cutdown"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pyqt6>=6.10.2",
]

[dependency-groups]
dev = [
    "pyqt6>=6.10.2",
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
]
